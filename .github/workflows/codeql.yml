name: CodeQL

on:
  # Not on pull_request: the slowest check (~4.5 min) would gate every merge,
  # while alerts from the push to main land in the Security tab all the same.
  push:
    branches: [main, dev]
  schedule:
    # Еженедельный полный проход, чтобы ловить новые правила даже без коммитов.
    - cron: "31 3 * * 1"

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (python)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: python
          queries: security-and-quality
          config-file: ./.github/codeql/codeql-config.yml

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:python"
