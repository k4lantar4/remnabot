---
name: فارسی‌سازی فوری fa-i18n
overview: "فارسی‌سازی فوری روی fa-i18n: P0 callback تقریباً تمام (غیر balance). P1 پیام چت در جریان. P2 منو/inline باقی. هر commit = حداکثر ۱ handler (+ fa.json). build+deploy+smoke اجباری. balance/** برای همیشه خارج از scope."
todos:
  - id: chunk0-cherry-pick
    content: الگوی 7f3596cc — انجام‌شده به‌صورت 68202cfc (fa.json +128، texts.format_traffic)
    status: completed
  - id: p0-callback-chunks
    content: "P0: callback.answer غیر-admin (بدون balance) — ۰ هاردکد باقی؛ ~۲۲ commit handler (۱۶ commit balance = legacy)"
    status: completed
  - id: p1-message-chunks
    content: "P1: answer/edit — ~۱۵ commit باقی (ترتیب فایل زیر؛ balance خارج scope)"
    status: completed
  - id: p2-menus-inline
    content: "P2: inline.py — ۵ دکمه هاردکد + reply.py در صورت نیاز"
    status: completed
  - id: utils-format-traffic
    content: "۱ commit اختیاری: app/utils/formatting.py — هنوز RU ثابت (Безлимит/ГБ)"
    status: completed
  - id: merge-smoke-main
    content: "پس از P1–P2: merge fa-i18n → dev-local → main فقط با تأیید صریح"
    status: completed
isProject: false
---

# پلن اجرایی فارسی‌سازی فوری (fa-i18n) — به‌روزرسانی audit

**تاریخ audit:** ۲۰۲۶-۰۶-۰۲ · **برنچ:** `fa-i18n` · **فلسفه:** fast-dirty-no-debt (بدون codemod، بدون admin localize، بدون refactor)

---

## ۱) جدول وضعیت فعلی (done vs remaining)

| حوزه | وضعیت | شواهد audit |
|------|--------|-------------|
| Git / deploy | **۱ commit جلوتر از `remnabot/fa-i18n`** (push نشده: `e5b7f6f8`) | `git status -sb`: `ahead 1` · `?? locales/` |
| Chunk 0 (الگوی `7f3596cc`) | **done** | `68202cfc` — `fa.json` +۱۲۸ کلید، `texts.format_traffic` چندزبانه · `7f3596cc` ancestor نیست ولی معادل روی برنچ است |
| P0 `callback.answer` (non-admin، **بدون balance**) | **done** | هاردکد بدون `texts.t`: **۰** · با fallback Cyrillic در `texts.t`: **۱۶۵** خط (کاربر fa از fa.json می‌خواند) |
| P1 `answer`/`edit` (non-admin، **بدون balance**) | **در جریان** | هاردکد بدون `texts.t`: **۷** خط · هرگونه Cyrillic در answer/edit: **۲۱۰** خط (عمدتاً f-string / قالب روسی) |
| `inline.py` دکمه `text=` هاردکد | **باقی P2** | **۵** دکمه (بدون `texts.*`) |
| `app/utils/formatting.py` | **باقی** | `format_traffic` هنوز `'Безлимит'` / `'{gb} ГБ'` ثابت |
| `texts.py` `format_traffic` | **done** | instance method با `fa`/`en`/… |
| نمایش قیمت **تومان** | **done** | `e5b7f6f8` — `PRICE_DISPLAY_SUFFIX` در `config.py` |
| `get_admin_texts` | **done** | `grep -r get_admin_texts app/` → **۰** |
| `fa.json` parity با `ru.json` | **done** | ۲۰۲۹ کلید fa · ۰ missing نسبت به ru |
| `app/handlers/balance/**` | **خارج scope دائمی** | ۱۶ commit legacy روی برنچ — **دیگر commit/work لیست نمی‌شود** |

### git — snapshot

```text
git log -5 --oneline
e5b7f6f8 Display prices with تومان suffix instead of ₽.
c777ad03 i18n(fa): localize MulenPay balance chat message
6fd02d1a i18n(fa): localize WATA balance chat message
23aa9a48 i18n(fa): localize Stars balance chat messages
4114571f i18n(fa): localize balance main chat messages

git status -sb
## fa-i18n...remnabot/fa-i18n [ahead 1]
?? locales/

unpushed vs remnabot/fa-i18n: 1 commit (e5b7f6f8)
```

---

## ۲) milestones تکمیل‌شده (تخمین تعداد commit)

| milestone | commit تقریبی | یادداشت |
|-----------|---------------|---------|
| Chunk 0 (`68202cfc` ≈ `7f3596cc`) | **۱** | ۲ فایل: `fa.json` + `texts.py` |
| P0 callback (handlers غیر balance) | **~۲۲** | از **۳۸** commit `i18n(fa)` — **۱۶** مربوط balance = legacy |
| P0 callback balance (legacy) | **~۹** | انجام شده؛ **دیگر ادامه نده** |
| P1 balance chat (legacy) | **~۷** | انجام شده؛ **دیگر ادامه نده** |
| P1 جزئی (غیر balance) | **۳** | `a4d87820` tariff chat errors · `c7ba342b` start web auth · `02ffad15` purchase toast |
| Currency تومان | **۱** | `e5b7f6f8` |
| **جمع done (قابل‌شمارش برای پیشرفت)** | **~۲۷ non-balance + ۱۶ balance legacy** | فقط **~۲۷** در گزارش پیشرفت کاربر؛ balance در جدول «انجام‌شده legacy» |

---

## ۳) فازهای باقی‌مانده — ترتیب دقیق فایل و اقدام per-file

**قانون هر commit:** حداکثر **۱** فایل `app/handlers/**` یا `app/keyboards/{inline,reply}.py` + `fa.json` در همان commit در صورت نیاز کلید · الگو: `texts.t('KEY', 'ru fallback')` · **بدون** balance.

### فاز P1 — پیام‌های `answer` / `edit_text` / `reply` (۱ فایل = ۱ commit)

ترتیب اجباری (بعد از smoke هر خط به خط بعد):

| # | فایل | اقدام per-commit |
|---|------|------------------|
| 1 | `app/handlers/subscription/tariff_purchase.py` | wrap f-stringهای کاربر با `texts.t` + KEY در fa.json؛ جایگزینی `from app.utils.formatting import format_traffic` در پیام‌های UI با `texts.format_traffic` یا صبر تا commit utils (ترجیح: همان commit فقط پیام‌ها؛ utils جدا) |
| 2 | `app/handlers/simple_subscription.py` | همان الگو — ~۲۴۷ خط Cyrillic «سخت» (بدون texts.t) |
| 3 | `app/handlers/subscription/purchase.py` | پیام‌های باقی‌مانده + خطوط مثل `f'📊 Трафик: …'` |
| 4 | `app/handlers/subscription/devices.py` | پیام rename/خطا/لیست دستگاه |
| 5 | `app/handlers/subscription/traffic.py` | پیام خرید/افزایش ترافیک |
| 6 | `app/handlers/start.py` | باقی‌مانده بعد از web auth (منو/خوش‌آمد) |
| 7 | `app/handlers/menu.py` | متن‌های `answer`/`edit` منو |
| 8 | `app/handlers/tickets.py` | تیکت — **P0 callback با texts.t هست**؛ تمرکز روی body پیام |
| 9 | `app/handlers/referral.py` | برداشت/رفرال |
| 10 | `app/handlers/stars_payments.py` | **۳** خط answer هاردکد + ~۵۱ خط Cyrillic سخت |
| 11 | `app/handlers/gift_activation.py` | **۳** خط answer هاردکد باقی + بقیه قالب‌ها |
| 12 | `app/handlers/webhooks.py` | **۱** خط answer هاردکد |
| 13 | `app/handlers/server_status.py` | وضعیت سرور |
| 14 | `app/handlers/subscription/autopay.py` | پیام لغو/تأیید autopay |
| 15 | `app/handlers/polls.py` | ۲ خط سخت باقی (اکثر callback انجام شده) |
| 16 | `app/handlers/promocode.py` | پیام‌های promocode |
| 17 | `app/handlers/common.py` | باقی‌مانده غیر ban-callback |
| 18 | `app/handlers/contests.py` | پیام منو/شرکت (callback P0 done) |

**تخمین P1 باقی:** **~۱۵–۱۸** commit (۱ فایل هر بار).

**فایل‌های با بیشترین Cyrillic در answer/edit (excl balance) — برای اولویت smoke:**

| فایل | خطوط answer/edit با Cyrillic (هر نوع) |
|------|--------------------------------------|
| `tariff_purchase.py` | ۷۸ |
| `purchase.py` | ۳۷ |
| `simple_subscription.py` | ۳۴ |
| `tickets.py` | ۱۵ |
| `referral.py` | ۹ |
| `start.py` | ۸ |

### فاز P2 — keyboards / منو

| # | فایل | اقدام |
|---|------|--------|
| 1 | `app/keyboards/inline.py` | ۵ دکمه: `Модерация`×۲، `Очистить корзину…`، `Подтвердить покупку`، `Очистить корзину` → `texts.t('KEY', 'ru…')` + fa.json |
| 2 | `app/keyboards/reply.py` | در صورت دکمه/برچسب هاردکد (۵ خط Cyrillic در scan) |

**تخمین P2:** **۲–۳** commit.

### فاز P1.5 اختیاری — utils

| # | فایل | اقدام |
|---|------|--------|
| 1 | `app/utils/formatting.py` | `format_traffic(gb, language=None)` → delegate به `get_texts(lang).format_traffic`؛ **۱۸** call در `tariff_purchase` از utils استفاده می‌کند |

**تخمین:** **۱** commit · بعد از یا همزمان با آخرین touch `tariff_purchase` (ترجیح: commit جدا طبق قانون ۱ فایل).

---

## ۴) چک‌لیست audit هر فاز (grep + Telegram)

### بعد از هر commit

```bash
docker compose build bot
docker compose up -d
docker compose ps   # healthy
docker compose exec bot python -c "import main"
cp app/localization/locales/fa.json ./locales/fa.json   # اگر fa.json عوض شد
docker compose restart bot
docker compose ps
grep -r get_admin_texts app/   # باید 0
```

### P0 gate (تأیید «تمام» — قبل از تمرکز کامل P1)

```bash
# هاردکد callback بدون texts.t — non-admin، بدون balance
rg "callback\.answer\(" app/handlers --glob '*.py' \
  | rg -v 'admin/' | rg -v 'balance/' \
  | rg '[\x{0400}-\x{04FF}]' | rg -v 'texts\.t' | wc -l
# انتظار: 0
```

### P1 gate (هر فایل بعد از commit همان فایل)

```bash
FILE=app/handlers/subscription/tariff_purchase.py   # مثال
rg "\.(answer|edit_text|edit_message_text|reply)\(" "$FILE" \
  | rg '[\x{0400}-\x{04FF}]' | rg -v 'texts\.t' | rg -v 'texts\.[A-Z_]' | wc -l
# هدف: 0 برای آن فایل (به‌جز logger/comment)
```

### P2 gate

```bash
rg "InlineKeyboardButton\(.*text=['\"]" app/keyboards/inline.py \
  | rg '[\x{0400}-\x{04FF}]' | rg -v 'texts\.' | wc -l
# انتظار نهایی: 0
```

### utils gate (پس از P1.5)

```bash
rg "def format_traffic" -A5 app/utils/formatting.py
# نباید 'Безлимит' / 'ГБ' ثابت برای fa بماند
```

### Telegram smoke (دستی — نمونه)

| فاز | مسیر smoke |
|-----|------------|
| عمومی | `/start` → متن فارسی |
| P0 | یک callback از همان handler commit‌شده → toast فارسی |
| P1 | همان فلو تا `answer`/`edit` → بدنه فارسی |
| P2 | منوی اصلی + دکمه تأیید خرید / پاک کردن سبد |
| Currency | قیمت با پسوند **تومان** (نه ₽) |
| Admin | پنل admin → بدون crash / NameError |

**Rollback:** فقط `git revert <one-sha>` — هر commit مستقل.

---

## ۵) Currency (تومان) — done + verify

| مورد | وضعیت |
|------|--------|
| Commit | `e5b7f6f8` — `Display prices with تومان suffix instead of ₽.` |
| فایل‌ها | `app/config.py` (`PRICE_DISPLAY_SUFFIX: str = ' تومان'`) · `app/localization/texts.py` · `app/utils/formatting.py` (delegate `format_price`) |
| Push | **۱ commit** ahead of `remnabot/fa-i18n` — push قبل از merge |
| Verify | `grep PRICE_DISPLAY_SUFFIX app/config.py` · smoke: خرید/موجودی/ترافیک — رقم + `تومان` |

**نیاز به commit جدید برای currency:** خیر (مگر regression).

---

## ۶) آمادگی merge (`fa-i18n` → `dev-local` → `main`)

**بدون تأیید صریح merge ممنوع.**

| معیار | شرط |
|--------|------|
| P0 non-admin | callback هاردکد = 0 (✓ الان) |
| P1 | همه فایل‌های جدول §۳ (غیر balance) — answer/edit هاردکد = 0 |
| P2 | inline ۵ دکمه = 0 |
| اختیاری توصیه‌شده | `utils/formatting.py` language-aware |
| Infra | `docker compose ps` healthy · `import main` OK |
| Locale mount | `./locales/fa.json` sync بعد از هر تغییر fa.json |
| Guards | `get_admin_texts` = 0 |
| Smoke | `/start` فارسی · subscription flow · admin بدون crash |
| Upstream | `git fetch` + بررسی conflict روی فایل‌های §۷ |
| Push | `remnabot fa-i18n` به‌روز (شامل `e5b7f6f8`) |

---

## ۷) فایل‌های پرریسک merge upstream

| ریسک | مسیر |
|------|------|
| **بالا** | `app/handlers/subscription/purchase.py` · `tariff_purchase.py` · `start.py` · `app/keyboards/inline.py` · `app/localization/texts.py` |
| **متوسط** | `devices.py` · `traffic.py` · `simple_subscription.py` · `menu.py` |
| **پایین** | `app/localization/locales/fa.json` (سفارشی) |

**استراتژی:** chunk کوچک + `git revert` تک‌commit؛ قبل از merge به `main` diff upstream روی ۵ فایل بالا.

---

## ۸) DO NOT TOUCH (صریح)

| ناحیه | مسیر / دلیل |
|-------|-------------|
| **Balance (دائمی)** | `app/handlers/balance/**` — commitهای legacy روی برنچ کافی؛ **هیچ کار/لیست باقی‌مانده** |
| Admin handlers | `app/handlers/admin/**` |
| Admin keyboards | `app/keyboards/admin.py` |
| `get_admin_texts` | ممنوع معرفی |
| codemod / refactor / multi-handler commit | قوانین سخت |
| localize admin | خارج scope |
| `ru.json` upstream | فقط `fa.json` |
| Log / structlog | |
| Docstring / comment | |
| `display_name_restriction` regex Cyrillic | عمدی |
| `MSG_*` انبوه / module-level `texts =` | |

---

## قوانین سخت اجرایی (بدون تغییر)

| قانون | جزئیات |
|--------|--------|
| فلسفه | fast-dirty-no-debt |
| Locale | فقط `fa.json` + حداکثر ۱ handler (+ fa.json همان commit) |
| الگو | `texts.t('KEY', 'ru fallback')` — fallback Cyrillic **بماند** |
| Chunk 0 مرجع | cherry-pick الگوی `7f3596cc` → انجام: `68202cfc` |
| گیت دروازه | build bot → up healthy → `import main` → cp fa.json → restart → `get_admin_texts=0` → Telegram smoke |
| PR/push | هر commit → `remnabot fa-i18n` |
| Rollback | `git revert <one-sha>` فقط |

---

## شاخص‌های audit خام (مرجع سریع)

| شاخص | مقدار |
|--------|--------|
| `callback.answer` Cyrillic، non-admin، **بدون balance**، **بدون texts.t** | **0** |
| `callback.answer` Cyrillic، non-admin، **بدون balance** (شامل fallback در `texts.t`) | **165** |
| `answer`/`edit` Cyrillic، non-admin، **بدون balance**، **بدون texts.t** | **7** (gift×3, stars×3, webhooks×1) |
| `answer`/`edit` Cyrillic، non-admin، **بدون balance** (هر نوع) | **210** |
| `inline.py` دکمه `text=` هاردکد Cyrillic | **5** |
| `formatting.format_traffic` | RU ثابت — **باقی** |
| `texts.format_traffic` | چندزبانه — **done** |
| commit `i18n(fa)` | **38** (+ `68202cfc` + `e5b7f6f8`) |

---

## کار عملیاتی خارج commit

- `cp app/localization/locales/fa.json ./locales/fa.json` پس از تغییر locale
- `?? locales/` در git status — sync/rm طبق infra محلی
- Permission: `chown 1000:1000 ./locales` در صورت نیاز
