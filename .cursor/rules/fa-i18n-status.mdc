---
description: fa-i18n living status — done, remaining, next slice (update after each session)
alwaysApply: true
---

# fa-i18n status (living doc)

**Update this file after every fa-i18n session** (5–10 lines in Done/Next). Do not re-run full Phase-0 discovery unless `main` diverged significantly or user asks.

## Decisions (stable)

| Topic | Decision |
|-------|----------|
| Admin group notifications | Persian (`AdminNotificationService`) |
| miniapp / cabinet UI | Later phase — out of scope |
| `./locales/` untracked | **Intentional** — Docker mount copy. Source of truth: `app/localization/locales/fa.json`. After fa.json change: `cp app/localization/locales/fa.json ./locales/fa.json` + restart bot. **Do not commit** `locales/`. |
| Admin panel language | `get_texts(db_user.language)` — not fixed ru |
| Disabled providers | Skip (CryptoBot, Heleket, WATA, Stars off in `.env`) |

## Git baseline

| Item | Value |
|------|-------|
| Production line | `remnabot/main` ≈ `dev-local` ≈ `fa-i18n` |
| **Branch** | `fa-i18n-traffic-ui` (P2 batch Jun 3 2026) |
| **HEAD** | `fc7eef0c` — see `git log -1` |
| Merge path | `fa-i18n-traffic-ui` → `fa-i18n` → `dev-local` → `main` after user OK |

## Done (P2 partial — Jun 3 2026)

- [x] **users.py U11–U16:** `78ab7ee0` search, `119da54f` per-user stats, `1594960e` buy-sub, `5ed67b86` buy-tariff, `78f8a77c` change-tariff, `c82606f8` autopay
- [x] **P2 admin:** `6645e29b` system_logs, `57c6c2da` bulk_ban, `e6991ff0` required_channels, `05c2122f` maintenance (panel core), `f264ca0a` rules, `fc7eef0c` updates
- [x] **User keyboards / units:** `3d1a2e9f`..`a169984f` traffic switch + `RESET_TRAFFIC_CONFIRM_BTN`; `8567004d` Persian period display
- [x] P1 (unchanged): user traffic/devices/countries, admin users U3–U10, tariffs T1–T12, notify N0–N8

## Next (P2 remainder — 10 modules, still 0× `texts.t`)

| Module | Plan id |
|--------|---------|
| `welcome_text.py` | P2-06 |
| `user_messages.py` | P2-07 |
| `subscriptions.py` | P2-09 |
| `statistics.py` | P2-10 |
| `blocked_users.py` | P2-11 |
| `blacklist.py` | P2-12 |
| `backup.py` | P2-13 |
| `campaigns.py` | P2-14 |
| `promocodes.py` | P2-15 |
| `servers.py` | P2-16 |

Also: finish `maintenance.py` callback toasts (toggle/monitoring/notify paths); optional P2.5 `referrals.py`, `remnawave.py`, `messages.py`.

## Smoke (every commit)

```bash
docker compose run --rm --no-deps bot python -c "import main"
cp app/localization/locales/fa.json ./locales/fa.json
docker compose restart bot
grep -r get_admin_texts app/  # must be 0
```

## Supersedes

P1 plans: `.cursor/plans/p1_admin_fa-i18n_a4ba6920.plan.md`, historical Phase-0 plan.
