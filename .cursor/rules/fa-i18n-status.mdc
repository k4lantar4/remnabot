---
description: fa-i18n living status — done, remaining, next slice (update after each session)
alwaysApply: true
---

# fa-i18n status (living doc)

**Update this file after every fa-i18n session** (5–10 lines in Done/Next). Do not re-run full Phase-0 discovery unless `main` diverged significantly or user asks.

## Decisions (stable)

| Topic | Decision |
|-------|----------|
| Admin group notifications | Persian (`AdminNotificationService`) |
| miniapp / cabinet UI | P1 user surfaces **merged** (Jun 6 2026); admin panels P2 |
| `./locales/` untracked | **Intentional** — Docker mount copy. Source of truth: `app/localization/locales/fa.json`. After fa.json change: `cp app/localization/locales/fa.json ./locales/fa.json` + restart bot. **Do not commit** `locales/`. |
| Admin panel language | `get_texts(db_user.language)` — not fixed ru |
| Disabled providers | Skip (CryptoBot, Heleket, WATA, Stars off in `.env`) |

## Git baseline

| Item | Value |
|------|-------|
| App version | **3.60.0** bot + **1.57.0** cabinet (upstream merge Jun 6 2026) |
| Production line | `remnabot/main` — local `main` at `03e1c2e8` (P1.5 merged) |
| **Merge branch** | `chore/merge-cabinet-1.57` @ `83f7e630` (bot 3.60 + cabinet 1.57) |
| **Bot merge** | `chore/merge-upstream-3.60` @ `3f6c8d23` |
| **Remotes** | `upstream` + `upstream-cabinet` (push blocked); see `docs/cabinet-upstream.md` |
| Alembic chain | `0087` → `0088` C2C → `0089` dedup → `0090` wheel idempotency |
| Upstream merge | 3.60: email-merge API, signed media token, Stars idempotency, wheel charge guard; cabinet 1.57: health blocking, gift claim, nav refresh |
| Merge path | `chore/merge-cabinet-1.57` → PR → `dev-local` → `main` (**awaiting user smoke + approval**) |

## Done (P2 — Jun 3 2026)

- [x] **P2-06..P2-16:** `8d429570` welcome_text, `29b9a6b5` user_messages, `c4e68831` subscriptions, `b144ef5f` statistics, `f69cc127` blocked_users, `e6373385` blacklist, `dfd8b941` backup, `0557b7f9` campaigns, `0394e7cc` promocodes, `26379aea` servers
- [x] **P2.5:** `756800f3` referrals, `0f6db44e` remnawave, `c9f6296d` messages
- [x] **maintenance P2-04 finish:** `2b865158` — panel, toggle, monitoring, notify paths
- [x] Prior P2: users U11–U16, system_logs, bulk_ban, required_channels, rules, updates; user traffic/units; P1 unchanged

## Done (P2.5 — Jun 3 2026)

- [x] `756800f3` `referrals.py` — full admin referrals/diagnostics/withdrawals (~201 `texts.t`)
- [x] `0f6db44e` `remnawave.py` — menu, auto-sync, nodes list, stats nav (~94 `texts.t`; large stat bodies still Cyrillic fallbacks)
- [x] `c9f6296d` `messages.py` — menu + pinned flows (~24 `texts.t`; broadcast FSM bodies still Cyrillic fallbacks)

## Done (upstream 3.59 — Jun 4 2026)

- [x] Merge upstream 3.59.0 on `chore/merge-upstream-3.59`; Alembic `0087` → `0088` (C2C) → `0089` (dedup no-op)
- [x] i18n audit post-merge: `get_admin_texts` = 0; `_admin_notify_texts()` intact; no i18n commits

## Done (P1 user surfaces — merged Jun 6 2026)

Branch `i18n/p1-user-surfaces` → `dev-local` → `main` (**30 commits**). Hot-path bot + cabinet + miniapp parity; `get_texts(user.language)` + `fa.json`.

- [x] **Bot purchase/subscription:** `purchase.py`, `tariff_purchase` (partial), `simple_subscription`, `menu`, `my_subscriptions`
- [x] **Bot traffic/devices/geo:** traffic, devices, countries, autopay, links
- [x] **Bot payments/UI:** stars_payments, start gifts, promo, promocode, tickets, inline keyboards
- [x] **Cabinet:** traffic, devices, purchase, tariff_switch, renewal, balance status, wheel, withdrawal, deps
- [x] **Miniapp:** user-facing API messages
- [x] **Fixes:** `5bf7481f` tariff NameError, `3dd41031` FIXED_TRAFFIC_LIMIT_GB, `48c252c4` format_balance `round_kopeks`

**P1 scope closed** — P1.5 remainder done on branch (see Done P1.5); P1.5b deferred (see Next).

## Done (P1.5 remainder — Jun 6 2026)

Branch `i18n/p1-remainder` from `main` (**10 commits**, `1ab9cb34` → `fe607943`). Slices 1a–6 complete.

- [x] **tariff_purchase.py:** instant/period switch success, ledger descriptions, renewal picker, unknown tariff name (`1ab9cb34`, `606ffa1e`, `6265f33e`, `232b66b2`)
- [x] **purchase.py:** paid trial payment UI labels (`457074fc`)
- [x] **devices.py:** change-prompt Persian text (`87856d82`)
- [x] **miniapp.py:** device API error messages (`b869e10f`)
- [x] **referral.py + fa.json:** `REFERRAL_WITHDRAWAL_ALL` key, withdrawal service reasons (`2789d995`, `ad4b73f7`)
- [x] **inline.py:** device count change keyboard labels (`fe607943`)

**Merged to `main`** — `03e1c2e8` (Jun 6 2026).

## Done (upstream 3.60 + cabinet 1.57 — Jun 6 2026)

Branch `chore/merge-cabinet-1.57` from bot merge tip (`4161455f` → `3f6c8d23` → `83f7e630`).

- [x] **Phase 0:** `4161455f` — `upstream-cabinet` remote, `docs/cabinet-upstream.md` archive-merge path
- [x] **Phase 1 bot:** `3f6c8d23` — fa.json overlay + upstream keys, C2C/toman/plugins preserved, Alembic 0090 wheel
- [x] **Phase 2 cabinet:** `83f7e630` — archive merge 1.57; `useCurrency.ts` skipFxConversion, fa toman, `verifyEmailMerge`, media token
- [x] Agent smoke: `import main` OK; cabinet `npm run build` OK (node:20-alpine)

**Not merged to `main`** — awaiting user smoke + explicit approval.

## Done (P1.5b — Jun 8 2026)

Branch `i18n/p1-remainder-b` from `main` at `3e61737c` (**3 commits**).

- [x] **simple_subscription.py 5a:** `44c57c75` — wrap YooKassa keyboard button texts (`SIMPLE_SUB_YOOKASSA_SBP_BTN`, `SIMPLE_SUB_YOOKASSA_CARD_BTN`)
- [x] **simple_subscription.py 5b:** `e273d747` — wrap YooKassa payment-flow buttons (`SIMPLE_SUB_YOOKASSA_PAY_LINK_BTN`, `SIMPLE_SUB_YOOKASSA_BANK_APP_BTN`)
- [x] **balance/main.py:** `5d83cf8b` — reuse `USER_RESTRICTION_DEFAULT_REASON` for restriction fallback
- [x] **menu.py / start.py:** no code changes — all 63 `texts.t()` keys already in fa.json with Persian values
- [x] fa.json: **+4 new keys** (total 4047); disabled-provider strings (CryptoBot, Heleket, WATA, Stars) skipped per `.env`

**P1.5b scope closed** — `i18n/p1-remainder-b` pushed to `remnabot`; awaiting user smoke + PR approval.

## Done (fa Jalali dates — Jun 9 2026)

Branch `i18n/fa-jalali-dates` — P1 user-surface Jalali calendar.

- [x] **Helper:** `app/utils/jalali_datetime.py` (`format_user_datetime`, `jdatetime` in pyproject/uv.lock)
- [x] **Bot:** `menu.py`, `start.py`, `my_subscriptions.py`, `purchase.py`, `simple_subscription.py`
- [x] **API:** `cabinet/routes/subscription_modules/purchase.py`, `webapi/routes/miniapp.py`
- [x] **Cabinet UI:** `formatDate.ts` + subscription pages + 8 user pages (Balance, Profile, Support, Referral, Gift, Merge, Wheel, SuccessModal)
- [x] **Tests:** `tests/utils/test_jalali_datetime.py` (anchor: 2026-07-09 → 18.04.1405 for fa)

## Done (emergency — Jun 9 2026)

- [x] **fix/remnawave-username-upstream-revert:** revert panel username to upstream create-only path — remove `build_multi_tariff_remnawave_username`, stop sending username on `update_user`, production `.env` → `user_{telegram_id}`
- [x] **chore/user-unknown-remediation:** regression test (`3734f5cf`), report CLI (`704898b2`), agent smoke — **scope closed** (Jun 9 2026 user approval). x-ui dry-run 0/44 match (post-bug multi-tariff subs, not migration legacy); 44 rows keep `{tariff} #{seq}` in UI via `2fcf103e`

## Done (fix/toman-balance-units — Jun 9 2026)

Branch `fix/toman-balance-units` from `main` — three cabinet balance unit fixes (Phase B parity).

- [x] **purchase.py:** IntegrityError refund uses `charge_toman` (same as subtract) — no 100× over-credit
- [x] **admin_bulk_actions.py:** `_do_add_balance` resolves `amount_display` via `_require_amount_kopeks`
- [x] **referral.py:** info + earnings `*_rubles` use `display_balance_from_storage`; `/terms` unchanged
- [x] **Tests:** `test_purchase_integrity_refund`, `test_bulk_add_balance_display`, `test_referral_display_units` — 5 passed

**Awaiting user smoke** before PR → `dev-local` → `main`. Follow-up: `withdrawal.py` `/100` display (out of scope).

## Done (fa-remaining — Jun 9 2026)

Branch `i18n/fa-remaining` from `main` (**9 commits**, `7b8d0155` → `e79e72e0`).

- [x] **Task 1:** cabinet fa locale integrity guards (`7b8d0155`)
- [x] **Task 2–4:** P1 cabinet user keys — subscription (75), gift/dashboard/notifications, resetPassword/balance/support (`1aed81fe`, `720a7ed3`, `93fb63d5`)
- [x] **Task 5–7:** P3 copywriting — خرید سرویس, revoke/reissue labels, user-facing تعرفه → سرویس (`0e2e893d`, `bb52b41f`, `a2c91aea`)
- [x] **Task 8:** P4 restriction fallback sweep via `USER_RESTRICTION_DEFAULT_REASON` (`071d4161`)
- [x] **Task 11:** P2 admin نود → سرور terminology + `ADMIN_RW_NODE_NOT_FOUND` fix (`e79e72e0`)
- [ ] **Deferred:** Task 9 `banSystem` (148 keys), Task 10 `admin.settings` (485 keys) — separate PR

**Awaiting user smoke** before PR → `dev-local` → `main`.

## Done (cyrillic-user-strings — Jun 11 2026)

Branch `i18n/fa-json-value-fixes` (includes `i18n/fix-subscription-status-display`).

- [x] **Slice 1:** `my_subscriptions.py` — `_subscription_status_display()` via `SUBSCRIPTION_STATUS_*` (fixes «وضعیت: 🟢 Активна») (`2a2f4adc`)
- [x] **Slice 2:** `fa.json` — `YuMoney`, withdrawal تومان + Iranian card example (`5020c8ec`)
- [ ] **Optional follow-up:** `formatters.py` — `fa` in `use_russian_fallback` for `format_subscription_status` / `format_traffic_usage` (not user hot path today; tests only)

## Done (monitoring notifications — Jun 16 2026)

Branch `i18n/monitoring-notifications` from `main`.

- [x] **Task 1:** `format_days_declension` fa branch (`N روز`) + test flip — fixes mixed «3 дня» in `SUBSCRIPTION_EXPIRING_PAID`
- [x] **Task 2:** `fa.json` — `SUBSCRIPTION_EXPIRED_NOTIFY`, `NOTIFY_TARIFF_LINE`, `NOTIFY_DAILY_DEBIT`, `NOTIFY_DAILY_INSUFFICIENT_FUNDS`
- [x] **Task 3:** `monitoring_service.py` — expired, trial (`TRIAL_ENDING_SOON` + `NOTIFY_TARIFF_LINE`), autopay-fail tariff/keyboard
- [x] **Task 4:** `daily_subscription_service.py` — daily debit + insufficient funds via `texts.format_price`/`format_balance`
- [x] **Task 5:** `tests/test_monitoring_notification_i18n.py` static guard
- [x] **Jalali follow-up:** `monitoring_service.py` — `format_user_datetime` for expiring/expired/discount `{end_date}`/`{expires_at}` (fa users); tests + `tools/send_monitoring_expiring_test.py`
- [ ] **Awaiting user smoke** before PR → `dev-local` → `main` (verify Jalali date in Telegram, not `17.06.2026`)

Plan: `docs/superpowers/plans/2026-06-16-fa-monitoring-notifications.md`

## Done (force default language — Jun 23 2026) — Phase 0 closed

Merged on `main` (**2 commits**; branch `fix/force-default-language-when-disabled` deleted).

- [x] **`d1df0821`** — `user_language.py` + `start.py` / `menu.py`: when `LANGUAGE_SELECTION_ENABLED=false`, resolve/persist `DEFAULT_LANGUAGE` (`fa`) for active users (DB had 1× `en`, 7465× `fa`)
- [x] **`881286bf`** — `texts.py` / `loader.py`: missing `fa` keys fall back to `ru`; `texts.t()` / attribute access no longer crash on absent keys
- [x] **Agent smoke + staging deploy** (2026-06-23): `uv run pytest tests/localization/*`, `make staging-rebuild`, `make staging-health`; smoke map `docs/templates/smoke-map.md`
- [x] **Production deploy** — `CONFIRM_PROD_DEPLOY=1 ./tools/deploy-production.sh`

## Done (remaining audit — Jun 23 2026)

Branch: audit-only (no code). Plan: `docs/superpowers/plans/2026-06-23-fa-i18n-remaining-audit.md`.

- [x] **Phase-0 metrics:** fa.json 4,158 keys, 0 Cyrillic values, 0 missing vs ru; user keys ~98% Persian
- [x] **Gap analysis:** 147 user `دستگاه` keys, 76 user `تعرفه` keys, duplicate «حجم» in traffic step (`tariff_purchase.py:386–393`)
- [x] **Fallback:** current `fa → ru` documented; target `fa → en → ru` + admin English track scoped
- [x] **Cabinet:** 96 user `دستگاه` paths, 26 Persian-digit user literals in `cabinet/src/locales/fa.json`
- [x] **Phase 0 closeout** — force-default-language + safe fa→ru fallback (merged on `main`)
- [x] **Phase 2 merged + prod** — bot device terminology (`a1375f5b`); user smoke تایید 2026-06-24
- [ ] **Implementation Phases 7–11:** **Phase 8 next** — `i18n/fa-cabinet-latin-digits`

## Done (Phase 7 cabinet device terminology — Jun 24 2026)

Branch `i18n/fa-cabinet-device-terminology` (**2 commits**, `c794d287` → `8a3b3b13`).

- [x] **Commit 1:** `cabinet/src/locales/fa.json` — 96 user paths `دستگاه` → `تعداد کاربر` / `اتصال` (`c794d287`)
- [x] **Commit 2:** `app/localization/locales/fa.json` — 20 `CABINET_*` API keys parity (`8a3b3b13`)
- [x] Guard: 0 user `دستگاه` in cabinet fa.json (admin 30 unchanged)
- [x] Agent smoke + cabinet `npm run build` + staging deploy
- [ ] **Awaiting user smoke** on staging cabinet before ship

## Done (Phase 6 تعرفه → سرویس — Jun 24 2026)

Branch `i18n/fa-tariff-to-service-json` (**1 commit**, `03d6743e`).

- [x] **fa.json:** 66 user keys `تعرفه` → `سرویس` (ADMIN_* unchanged); guard 0 user keys remaining
- [x] Agent smoke: `import main` OK
- [x] Staging deploy + smoke map
- [x] User smoke **تایید** 2026-06-24
- [x] **Merged** PR [#80](https://github.com/k4lantar4/remnabot/pull/80) → `main` @ `97634051`; prod deploy 2026-06-24

## Done (Phase 5 traffic-first funnel UX — Jun 24 2026)

Branch `i18n/fa-traffic-step-ux` (**6 commits**, `22ca0bc6` → `9538e469`); pushed; staging deployed.

- [x] Tariff list: drop GB/device line; show admin `description` for fa
- [x] Volume step: `TARIFF_TRAFFIC_STEP_INTRO` (no default 1 GB); hint copy; buttons unchanged
- [x] Period step: `TARIFF_PERIOD_STEP_*` hint (no formula in message); buttons unchanged
- [x] Pre-invoice: `پیش‌فاکتور` / سرویس / حجم / مدت labels in fa.json + `purchase_confirm.py`
- [x] Agent smoke + staging bot rebuild + `make staging-health`
- [ ] **Awaiting user smoke** on `@mrj7_bot` before ship

## Done (Phase 3 purchase success copy — Jun 24 2026) — CLOSED

Branch `i18n/fa-copy-purchase-success` (**3 commits**, `35391a7e` → `5e3061d4`).

- [x] **Commit 1:** `POST_PURCHASE_ONBOARDING` + 8 `TARIFF_*_SUCCESS` keys, `TARIFF_INFO_HEADER`, `CABINET_PURCHASE_TARIFF_SUCCESS`, `SIMPLE_SUB_PAYMENT_*` (`35391a7e`)
- [x] **Commit 2:** `tariff_purchase.py` — `_with_post_purchase_onboarding()` at 8 success sites (`e2751e1d`)
- [x] **Commit 3:** cabinet `successNotification` — `سرویس`, `رفتن به اشتراک من` (`5e3061d4`)
- [x] Agent smoke + staging deploy
- [x] User smoke **تایید** 2026-06-24 (Happ app name fix before ship)
- [x] **Merged** PR [#75](https://github.com/k4lantar4/remnabot/pull/75) → `main` @ `43956c0f`; prod deploy 2026-06-24

## Done (Phase 2 device terminology — Jun 24 2026) — CLOSED

**Merged** PR [#73](https://github.com/k4lantar4/remnabot/pull/73) → `main` @ `a1375f5b` (2026-06-23); **prod deploy** + user smoke **تایید** (2026-06-24).

Branch `i18n/fa-device-terminology-bot` (**4 i18n commits** + docs `c61ed35b`).

- [x] **Commit 1:** TARIFF_* purchase/confirm/success — `👥 تعداد کاربر` (`e239aee1`)
- [x] **Commit 2:** CHANGE_DEVICES_*, DEVICE_*, DEVICES_* — `کاربر` / `اتصال` (`d3b79ee0`)
- [x] **Commit 3:** MY_SUB_* labels (`1d3e2148`)
- [x] **Commit 4:** SUBSCRIPTION_*, CAMPAIGN_*, PURCHASE_CART_* overview keys (`efb1ca2b`)
- [x] User «دستگاه» bot keys: **147 → 9** (remaining: HAPP_*, SUBSCRIPTION_CONNECT_*, MINIAPP_* — Phases 9/13; CABINET_* done Phase 7)
- [x] Hotfix same session: PR [#74](https://github.com/k4lantar4/remnabot/pull/74) `show_main_menu` NameError (`9fa5cef5`)

**Out of Phase 2 scope (separate phases):** cabinet `fa.json` device labels (Phase 7), miniapp (Phase 9), platform picker (Phase 13).

## Done (Phase 4 my_subscriptions onboarding — Jun 24 2026)

Branch `i18n/fa-onboarding-my-sub` (**8 commits**, `2c572ed0` → `f8e27dda`).

- [x] **Phase 4:** fa.json onboarding keys + `my_subscriptions.py` detail block, first-connect hint, setup guide row
- [x] **Phase 4b:** mini-app aligned copy (`POST_PURCHASE_ONBOARDING`, `MY_SUB_DETAIL_ONBOARDING`, `SUBSCRIPTION_CONNECT_MINIAPP_MESSAGE`)
- [x] **Phase 4b:** `resolve_connect_webapp_url()` — cabinet `/connection?sub=` when RemnaWave apps exist, else sub link
- [x] **Phase 4b:** removed duplicate connect button on detail keyboard
- [x] **Phase 4c:** bot detail labels (`MY_SUB_DETAIL_HEADER`, `MY_SUB_BTN_TRAFFIC`, `MY_SUB_BTN_AUTOPAY`)
- [x] **Phase 4c:** cabinet subscription page — `حجم` label, `volumeEmptyHint`, connect guide CTA (no device count on card)
- [x] Agent smoke + staging deploy (2026-06-24)
- [ ] **Awaiting user smoke** on `@mrj7_bot` + staging cabinet before ship

## Next

**Status file for new session:** `.cursor/rules/fa-i18n-status.mdc` (this file).

**Priority implementation** (audit plan §9): **Phase 8** (`i18n/fa-cabinet-latin-digits`) → Phase 9 miniapp parity.

**fa Jalali P1 complete** on branch `i18n/fa-jalali-dates` — pending user smoke before PR → `dev-local` → `main`.

### user_unknown remediation — CLOSED (do not reopen)

| Out of scope (explicit) | Reason |
|-------------------------|--------|
| RemnaWave panel rename (`user_unknown_*` → `user_{telegram_id}_{short_id}`) | Triggers `user.modified` per user; cosmetic panel label only |
| `build_multi_tariff_remnawave_username` / `username` on `update_user` | Caused Jun 9 `#modified` flood; reverted in PR #49 |
| x-ui `--execute` for 44 `user_unknown_*` rows | 0/44 matcher hits — not migration legacy; wrong tool |
| Separate tracks | `i18n/fa-remaining` Task 9/10, `fix/toman-balance-units`, other open PRs |

**Operator inventory:** `docker compose run --rm bot python tools/report_user_unknown_subscriptions.py`

### Post-P1.5b

| Priority | Item | Notes |
|----------|------|-------|
| 1 | PR `i18n/p1-remainder-b` → `dev-local` → `main` | **Awaiting user smoke + approval** |
| — | `app/cabinet/routes/withdrawal.py` | optional language-parity follow-up |

### P2 admin (separate)

Plan: `docs/superpowers/plans/2026-06-06-fa-i18n-p2-admin-bot.md`. Email templates: separate plan.

## Smoke (every commit)

See `delivery-cycle.mdc` and `localization-upstream.mdc`:

```bash
docker compose run --rm --no-deps bot python -c "import main"
cp app/localization/locales/fa.json ./locales/fa.json
docker compose build bot cabinet-frontend && docker compose up -d bot cabinet-frontend
grep -r get_admin_texts app/  # must be 0
```

Currency: layer 2 `currency-display-toman.mdc`; FX layer 3 `currency-fx-boundaries.mdc` — never both in one commit.

## Supersedes

P1 plans: `.cursor/plans/p1_admin_fa-i18n_a4ba6920.plan.md`, historical Phase-0 plan.
